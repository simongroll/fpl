Meteor.startup(function() {

timeSinceLastUpdated = "Never";
currentlyUpdating = false;

    // Meteor.call("loadTeamData", function(err, res) {
    //     if (err) console.log(err);
    //     else {}
    // });

    // SyncedCron.start();

});
